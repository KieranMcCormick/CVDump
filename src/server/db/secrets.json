{
    "production": {
        "user"     : "project_user",
        "password" : "sup3rsecure",
        "database" : "project",
        "socket"   : "/var/run/mysql-db/mysqld.sock"
    },
    "test": {
        "user"     : "project_user",
        "password" : "sup3rsecure",
        "database" : "project_test",
        "socket"   : "/var/run/mysql-db/mysqld.sock"
    }
}
